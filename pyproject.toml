[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "wordcount"
version = "0.1.0"
description = "A tiny text-stats library."
requires-python = ">=3.10"

[tool.setuptools]
py-modules = ["wordcount"]
